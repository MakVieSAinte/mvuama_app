// Ce fichier est obsolète et n'est plus utilisé
// Les routes utilisateur ont été intégrées directement dans le fichier src/router/index.ts
// Vous pouvez supprimer ce fichier

// Export un tableau vide pour éviter les erreurs d'importation
const userRoutes = [];

export default userRoutes;
    ]
  }
];

export default userRoutes;
